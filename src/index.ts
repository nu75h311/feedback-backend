// eslint-disable-next-line no-unused-vars
import * as _ from './server/env';
import { serve } from './server';
// import { connectDb } from './models';

// connectDb().then(async () => {
//   await app.server;
// });
serve;
